import { TitleForm } from "../Login/LoginForm/style";

const NotFound = () => {
  return (
    <section className="container">
      <TitleForm>Error:404</TitleForm>
      <p>Página não encontrada</p>
    </section>
  );
};

export default NotFound;
